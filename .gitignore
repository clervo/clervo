# Secrets and local environment
.env
.env.*
!.env.example
secrets/
private/

# Dependencies and generated output
node_modules/
.venv/
vendor/
dist/
build/
release-artifacts/
coverage/
__pycache__/
*.pyc
*.egg-info/
*.blend1
apps/site/public/
apps/site/dist-server/

# Visual-regression captures. Baselines are 25 MB of PNGs that are regenerated
# from the build, so they are a local artifact rather than source: capture with
# `--update` before a change, then run the harness after it to see the drift.
apps/site/visual-baseline/
apps/site/visual-output/

# Runtime state
data/
tmp/
.staging-state/
*.log
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Editors and operating systems
.DS_Store
.idea/
.vscode/
*.swp
