[build-system]
requires = ["setuptools==83.0.0", "wheel==0.47.0"]
build-backend = "setuptools.build_meta"

[project]
name = "clervo-sdk"
version = "0.4.2"
description = "Python SDK for Clervo AI and agent tools with explicit x402 payment controls and replay-safe requests."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Clervo" }]
keywords = ["clervo", "ai-agents", "x402", "search", "sdk"]
classifiers = [
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Typing :: Typed",
]

[project.urls]
Homepage = "https://clervo.dev/docs/python"
Documentation = "https://clervo.dev/docs/python"
Issues = "https://github.com/clervo/clervo/issues"
Repository = "https://github.com/clervo/clervo"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools]
license-files = ["LICENSE"]

[tool.setuptools.package-data]
clervo = ["py.typed"]
