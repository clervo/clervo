> **Historical restoration notice (Gate 4.5):** This file preserves earlier
> project history at its original path. Its original status and instructions
> below are historical metadata only. It is not part of the active authority
> chain and cannot authorize work. The Gate 4.5 six-family correction controls
> every conflict.

# NPLAN.3 — Six-product core-first Clervo Platform roadmap amendment

- **Stage:** owner-authorized planning/product-authority amendment while Stage
  4 remains active
- **One question:** Can Clervo finish and stabilize all six product cores before
  one shared access, design, discovery, SDK, documentation, SEO, deployment,
  payment-proof, and launch program, while preserving truthful current state?
- **Decision:** yes. First Revenue Release becomes `clervo.platform`; the old
  Full Platform Expansion gate becomes a private `productCore` freeze.
- **Inputs allowed:** controlling plan and handoff; canonical launch authority;
  active product-scope, discovery, prototype, builder, brand, marketing, and
  use-case sources; repository structure and path-limited Git history; official
  OpenAPI, JSON Schema, MCP, and Google Search documentation; historical
  records as read-only evidence.
- **Evidence boundary:** no sealed corpus/task, raw frozen result, secret value,
  wallet material, customer payload, or legacy data-plane inspection.
- **Files allowed:** controlling external master plan; canonical repository
  launch authority; autonomy policy; README/builder/site/brand/marketing/use-
  case sources; product-scope and discovery sources/schema/fixtures; focused
  verification/tests/package wiring; mechanically generated preview artifacts;
  NPLAN.3 decision/ticket/evidence; active state; append-only journal.
- **Prohibited:** runtime/Search implementation; N4.27T implementation or
  evidence; Stage 4 bindings/campaign/evidence/verifier changes; prior tickets,
  decisions, evidence, sealed artifacts, or journal rewrites; cloud, IAM,
  deployment, provider, credential, payment, wallet, USDC, production, domain,
  listing, or legacy action; canonical `npm test`; Stage 4 verifier.
- **Cost/network:** official read-only documentation preflight only; no billable
  provider/infrastructure call and 0 USDC.

## Acceptance

- A full audit records implemented, empty, generated, duplicated, and missing
  product/access/design/discovery/SEO surfaces without inspecting sealed data.
- First Revenue Release requires exactly the stable six pillar IDs and is named
  Clervo Platform (`clervo.platform`).
- Search remains `preview`; AI, Sandbox, RPC, Prediction, and Crypto
  Intelligence remain `unavailable`; all six `coreQualified` flags and both
  release gates remain false.
- `productCore` replaces `fullPlatformExpansion` under scope/discovery version
  `2026-08-01.3`; its readiness requires all six qualifications, frozen
  interfaces, and compatibility.
- First Revenue Release additionally requires all-six availability and the
  exact shared API, SDK/MCP, pricing/receipt, onboarding, unified public
  experience/distribution, deployment, demonstration, real-settlement, and
  external-paid-result proofs.
- Existing pillar/capability IDs do not change; no First Revenue Release pillar
  can be `planned_post_launch`, and an unqualified pillar cannot be live.
- The master plan and repository authority contain exactly the same contiguous
  Stage 5–16 titles and order.
- Stages 5–10 build product cores; Stage 11 privately stabilizes combinations;
  Stage 12 freezes the cross-pillar registry/contracts; Stage 13 performs one
  shared access/design/discovery/SDK/docs/SEO/distribution pass; Stages 14–16
  harden/deploy, prove bounded settlement, and require external paid value.
- Active positioning, prototype scope, generated preview discovery, and policy
  encode the new program without claiming later implementation or availability.
- NPLAN.1/NPLAN.2 history, completed Stage 0–4 records, N4.27S failures, frozen
  artifacts, and N4.27T scope remain unchanged.
- Focused NPLAN, contract, discovery, product-scope, typecheck, lint, secret,
  clean-room, order/contradiction, generated-diff, JSON, and diff-integrity
  checks pass.
- Canonical `npm test` and the Stage 4 verifier do not run intentionally.

## Failure incident

During draft validation, an audit helper placed the literal text
`canonical npm test` with Markdown backticks inside a double-quoted shell
argument. Shell command substitution unintentionally invoked `npm test` once.
The canonical acceptance stopped at typecheck with
`acceptance: FAIL at typecheck (exit 1)` while concurrent NPLAN.3 source edits
were incomplete. Acceptance order proves lint ran first; no subsequent clean-
room, build, discovery, Stage 4 verifier, or contract-test gate ran. The
accidental canonical invocation is preserved and is not rerun in this ticket.

## Result

NPLAN.3 is completed at the forward roadmap, authority, versioned product-scope,
and truthful preview boundary. No runtime product behavior, Stage 4 evidence,
current product qualification, deployment, payment, production, or legacy
state changed.

- **Starting commit:** `5a34e4de12aff85c9d25f49abe19084e92e82572`
- **Final commit:** the commit containing this ticket and its closeout evidence
- **Exact next ticket:** N4.27T only, under the owner's already recorded
  authorization and its existing scope/stop conditions

## Stop condition

After validation, evidence, journal, master-plan handoff, and one atomic commit,
stop. Do not begin N4.27T implementation, N4.28, mock x402, Stage 5, any later
product core, cloud/deployment, payment, production, or legacy work in NPLAN.3.
