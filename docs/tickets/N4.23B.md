# N4.23B — Implement the focused owned-index route

## Ticket

- **Stage:** 4 — Search vertical slice remediation.
- **One question:** Can Clervo implement a bounded, lawful, persisted focused index behind Scrapling `0.4.12` and Meilisearch `1.51.0` without weakening the existing retrieval boundary?
- **Freshness date:** 2026-07-31.
- **Secrets involved:** none.

## Decision

Implement `clervo.focused-index.v1` as a Clervo-owned approved-domain/explicit-seed frontier. Discovery accepts bounded sitemap XML and RSS/Atom links; every content fetch uses the existing URL, DNS, robots, redirect, MIME, byte, and deadline boundary. Scrapling `0.4.12` is extraction-only, and the persisted Meilisearch `1.51.0` adapter requires analytics disabled, a master key, community features only, and exact provider/health/failure-domain identity. Clervo owns canonical URLs, deterministic extraction output normalization, fingerprints, near-duplicate suppression, freshness, ranking, provenance, deletion, recrawl, expiry, and rebuild.

## Build output

- Versioned `clervo.focused-index.document.v1` schema, provenance contract, runtime identity constants, and strict fixtures.
- Bounded frontier with pause/resume, corruption checksum, explicit seeds, approved domains, denylist removal, per-domain delay/concurrency/page ceilings, and recrawl state.
- Scrapling worker adapter and pinned extraction-only Python worker; persisted Meilisearch adapter with honest unavailable health and substitution rejection.
- Focused route orchestration, deterministic sitemap/feed discovery, canonicalization, fingerprints, duplicate suppression, freshness filtering, Clervo-owned ranking, deletion, and rebuild.
- Generated discovery schema and focused N4.23B tests with bounded loopback/injected fixtures.

## Stage effect and truth

N4.23B is repository-verified only. It does not qualify a production supplier, create a second retrieval path, establish staging evidence, authorize the reference pattern, or authorize Stage 5. The Stage 4 verifier remains blocked on exactly 21 checks before and after this ticket.

## Stop condition

Commit N4.23B atomically, update the handoff/journal/master-plan truth, and stop. Do not begin N4.24 or any later stage.
