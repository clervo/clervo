# N4.27T — Frozen-result failure remediation

**Status:** completed on 2026-08-01 with one failed isolated cloud
qualification preserved and all ticket resources cleaned

**Stage:** 4 — Search vertical slice

## Authority

The owner explicitly authorized N4.27T under its recorded scope and stop
conditions. NPLAN.4 admitted the repository-local cycle. The owner subsequently
granted the configured deployer access and explicitly ordered execution after
receiving the exact project, resources, USD 5 gross and daily ceilings, one-run
limit, cleanup and prohibited-action plan. The runtime cannot inspect
promotional-credit expiry, so the owner-authorized USD 5 gross attached-billing
ceiling controls regardless of credit application. This does not authorize
IAM or billing mutation.

## One question

Can Clervo produce a new independently pre-split, cloud-ready repair for the
three failure classes identified by the immutable N4.27S decision—developer
registry retrieval, browser/hostile reliability, and resource-overlap cost
enforcement—without opening, rerunning, modifying, relabelling, or tuning
against N4.27S final evidence?

## Protected evidence boundary

The following are protected and remain untouched:

- `benchmarks/n4.27s/**`;
- `docs/evidence/n4.27s/final-quality/**`;
- the N4.27S final run marker, evaluator output, raw results and scorecard;
- N4.27S deployment and qualification artifacts except the public decision and
  truthful summary used to identify the three repair classes; and
- every N4.27/N4.27R sealed or once-only artifact.

N4.27T created a new procedure under `benchmarks/n4.27t`. Development and
validation material were split and hash-bound before implementation. Repair
used only the development split. The validation split then executed exactly
once under the separately authorized isolated qualification. It is final and
must never be rerun, reused or tuned against.

## Local build output

1. A deterministic pre-split developer/browser qualification corpus and freeze
   manifest with disjoint development and validation identities.
2. A developer-registry adapter that prefers exact npm package and GitHub
   repository metadata endpoints, uses bounded search only when an exact
   identity is unavailable, and preserves quota, terms, attribution and zero
   provider-API cash cost.
3. A disposable Chromium worker/supervisor contract with explicit preflight,
   render and cleanup budgets; init/reaping; dedicated shared memory; non-root,
   read-only and gateway-only boundaries; deterministic JavaScript and hostile
   fixtures; and immutable safe receipts.
4. A fail-closed resource-exclusivity gate that rejects creation while any
   ticket-owned chargeable resource, deletion operation, unknown inventory or
   combined daily exposure could violate the finite ceiling.
5. Focused adversarial tests and a local readiness record. No staging or product
   claim is promoted.

## Allowed paths

- `benchmarks/n4.27t/**`
- `scripts/benchmarks/n4.27t/**`
- `infra/n4.27t/**`
- `apps/api/src/n427t-staging-main.mjs`
- `tests/contract/n4.27t.test.mjs`
- `tests/contract/n4.27t-cloud.test.mjs`
- `tests/contract/nplan.4.test.mjs` (transition-regression repair only)
- `docs/tickets/N4.27T.md`
- `docs/decisions/N4.27T-LOCAL-REPAIR-DESIGN.md`
- `docs/evidence/N4.27T-local-repair-readiness.md`
- `docs/evidence/n4.27t/**` (new cloud freeze, raw result and cleanup evidence only)
- `docs/decisions/N4.27T-CLOUD-QUALIFICATION-DESIGN.md`
- `infra/control-plane/autonomous-dispatch-state.json`
- `package.json`
- `README.md`
- `AI_BUILDER.md`
- `apps/site/PROTOTYPE-COPY.md`
- `docs/operations/AUTONOMOUS-COMPLETION.md`
- `docs/product/CLERVO-LIVE-INTELLIGENCE-LAUNCH-AUTHORITY.md`
- `docs/product/INITIAL-AGENT-USE-CASE-FRAMEWORK.md`
- `docs/marketing/INITIAL-COMMERCIAL-RELEASE.md`
- `docs/journal/ACTIVE-TICKET-STATE.md`
- append-only `docs/journal/BUILD-JOURNAL.md`
- the external master-plan handoff at closeout

Existing N4.27S implementation and evidence files are read-only.

## Validation

- `npm run test:n4.27t`
- `npm run typecheck`
- `npm run lint`
- `npm run verify:autonomous-completion`
- `npm run verify:product-scope`
- `npm run scan:secrets`
- `npm run verify:boundary`
- Node syntax, JSON parse, artifact hash and Git diff-integrity checks

Do not run canonical `npm test`, the Stage 4 exit verifier, N4.27S final
qualification, mock x402, any payment flow, or any external deployment in this
local phase.

## Cost, network and secret boundary

- Mandatory paid/eventually-paid/trial-to-bill provider API cash spend: USD 0.
- Local implementation and recorded-fixture tests only after the official-doc
  preflight; no credentialed provider call.
- Cloud/IAM/billing/secret mutation: prohibited.
- USDC spend: 0; the 0.03 USDC reserve remains untouched.
- No secret values, wallet material, customer payloads or legacy runtime use.

## Isolated qualification result

The one-run Kubernetes Job completed without restart on the exact frozen image.
Developer registry retrieval passed 10/10. Browser qualification failed 0/20:
all 12 JavaScript and eight hostile fixtures returned
`browser_process_failed:`. Therefore `finalGatePass` is false. The raw result,
Kubernetes execution and cleanup are independently hash-bound by
`docs/evidence/n4.27t/qualification-closeout.v1.json`.

The namespace, cluster, image and Artifact Registry repository were deleted
synchronously and fresh exact-prefix inventory found zero residual resources
or pending operations. Estimated gross cost is USD 0.04854 (reported as USD
0.05) against the USD 5 ceiling; actual billed cost is unknown because the
environment blocks billing inspection. Provider API cost and USDC spend were
zero.

## Stop condition

Commit this failed qualification and cleanup evidence, mark N4.27T completed,
and stop. Hand off to a fresh N4.27U local repair cycle. Do not rerun N4.27T,
create new cloud resources, begin N4.28, run mock x402, start Stage 5, use real
payment, modify production, or touch legacy state.
