# N4.22 — Source-bound Stage 4 remediation campaign control

## Ticket

- **Stage:** 4 — Search vertical slice remediation.
- **One question:** Which exact 21 source-bound checks remain, what evidence closes each, in what dependency order, and can the campaign proceed from this container without mislabeling local evidence as staging proof?
- **Freshness date:** 2026-07-31.
- **Secrets involved:** none; only credential environment-variable names and credential-presence booleans were inspected.

## Decision

Create one machine-readable blocker matrix and deterministic verifier before further implementation. The matrix must exactly equal the Stage 4 verifier's current blocking identifiers, require staging in every resolution boundary, bind the TypeScript conclusion to workspace configuration, topologically validate the campaign queue, and fail closed if external gates are silently promoted.

N4.23 cannot begin because the container has no active gcloud identity or application-default credentials, the private service rejects unauthenticated access, no production/resale-qualified two-path supplier decision exists, public Nominatim is prohibited for resale, Common Crawl requires legal approval, and payable-route/alert-channel authorization is absent.

## Build output

- `infra/staging/stage4-remediation-campaign.json`: 21 individual blocker records, grouped N4.23–N4.28 queue, TypeScript conclusion, staging-access observation, and exact owner intervention.
- `scripts/verify-stage4-campaign.mjs`: source-bound validation against the real Stage 4 verifier inputs, package TypeScript version, and `tsconfig.json`.
- Five deterministic focused tests for exact blocker coverage, identity substitution, local/staging confusion, TypeScript target downgrade, dependency ordering, and external-gate promotion.
- Human campaign evidence and synchronized handoffs. No Stage 4 check or staging-verification boolean is changed.

## Validation

- `npm run typecheck`: passed with TypeScript 7.0.2 and `ES2023`; no repository change required.
- `npm run test:n4.22`: passed 5/5.
- Canonical `npm test`: passed 205/205 with `acceptance: PASS`, 36 schemas / 67 fixtures, zero external network calls during acceptance, and 0 USDC spent.
- `npm run verify:stage4-exit`: passed as a verifier execution with decision `blocked`, 21 blocking checks, reference-pattern authorization false, and Stage 5 authorization false.
- `npm run lint`: passed 182 source/contract files.
- `git diff --check` and the clean-room boundary verifier: passed at closeout.

## Stop condition

Commit N4.22, synchronize the writable external master plan, report the genuine external blocker, and stop. Do not begin N4.23 or Stage 5 until the named owner prerequisites are supplied.
