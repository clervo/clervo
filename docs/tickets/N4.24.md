# N4.24 — Independent live-federation route and connected local retrieval pipeline

## Ticket

- **Stage:** 4 — Search vertical slice remediation.
- **One question:** Can Clervo connect an independent, lawful live-federation route to the focused-index route through one bounded deterministic local retrieval pipeline without identity substitution or unsafe source use?
- **Freshness date:** 2026-07-31.
- **Secrets involved:** none.

## Decision

Implement `clervo.live-federation.v1` with its own provider, adapter, health, circuit, and failure-domain identities. The smallest qualified open-data set is Wikimedia Action API metadata plus Crossref REST bibliographic metadata. Wikimedia output retains page URL, attribution, and page-level license obligations; Crossref abstracts and publisher full text are excluded because rights may be unresolved. Common Crawl CDXJ/index records are metadata-only discovery and never archived body output; unresolved archived-content rights fail closed. Current publisher pages are fetched only through the existing Clervo URL/DNS/robots/redirect/MIME/byte/deadline boundary.

The connected pipeline performs deterministic query rewriting, concurrent route execution, bounded timeout/cancellation, exact route accounting, canonicalization, exact/near-duplicate suppression, freshness/authority/relevance/domain-diversity ranking, language/region propagation, extraction provenance, offset-bound citations, prompt-injection isolation, deterministic replay/schema validation, and honest `ready`/`degraded` behavior. Both route failures fail closed. `search.web`, `web.fetch`, and `web.extract` are locally callable; `search.answer` remains preview-only and `research.report` unavailable.

Crawl4AI `0.9.2` with Playwright `1.61.0` is a provisional internal fallback selected only by deterministic JavaScript-required evidence. It has no public raw API, arbitrary JavaScript, hooks, LLM integrations, downloads, stealth, proxy, or persistent browser state; N4.25 remains responsible for production isolation qualification.

## Build output

- Reviewed open-data adapter identities and source-use/attribution requirements.
- Wikimedia, Crossref, and metadata-only Common Crawl CDXJ adapters with fail-closed unresolved policy.
- Independent live-federation route, health/circuit state, direct current-page fetch wrapper, and provisional Crawl4AI selection/attestation.
- Connected route coordinator and response contract with route identity in attempts/results/citations.
- Local `search.web`, `web.fetch`, and `web.extract` behavior with deterministic merge/rank/citation and preview/unavailable boundaries.
- Connected response schema/fixtures, generated discovery schema, and focused N4.24 tests.

## Stage effect and truth

N4.24 is repository-verified only. It does not promote staging evidence, close source-bound Stage 4 blockers, authorize the reference pattern or Stage 5, qualify production/resale supply, qualify Crawl4AI isolation, enable payment, or permit archived Common Crawl bodies in paid/production output. Stage 4 remains blocked on exactly 21 checks before and after this ticket.

## Stop condition

Commit N4.24 atomically, update the handoff/journal/master-plan truth, and stop. Do not begin N4.25 or any later stage.
