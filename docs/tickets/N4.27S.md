# N4.27S — Frozen-repair staging qualification and remaining non-payment Stage 4 proof

Status: completed with a failed staging qualification on 2026-08-01. This
ticket began at commit `e8272d002a6429067d7e8ee5dcad989a0f503af1`
with a clean worktree.

## Objective

Deploy the exact N4.27R repair into isolated private staging and determine
whether quality, network-inclusive latency, browser reliability, source mesh,
citations, safety, connected cache, locale behavior, and operational controls
survive real cloud and public-network conditions.

This is a qualification ticket. Ranking weights, evaluator rules, source
choices, or task-specific behavior may not be changed after the final staging
qualification. Any pre-final staging correction must be independently
justified, covered by focused tests, recorded, and included in a new freeze.

## Authority and ceilings

- Google Cloud project: `bloxsniper-prod`.
- New resources: names beginning `clervo-n427s-` only.
- Private application ingress; isolated from legacy runtime, data, wallets,
  queues, databases, service accounts, and secrets.
- Digest-pinned images, least privilege, explicit ceilings, and teardown after
  evidence capture.
- Maximum gross ticket spend USD 12; maximum configured daily exposure USD 5;
  third-party general-Web provider production cost USD 0; USDC spend 0.
- Mock x402, real payment, N4.28, Stage 5, public DNS, website work, and legacy
  mutation are prohibited.

## Required proof

1. Preserve both prior one-run benchmark artifacts and verify every N4.27R
   implementation-freeze hash without rerunning either sealed set.
2. Freeze at least 50 unseen real-public-source tasks, at least ten per product
   family, with versioned labels, source dates, manual or deterministic label
   validation, hostile fixtures kept separate, and a one-run final marker.
3. Privately deploy the frozen focused index, repaired federation/RRF path,
   qualified zero-provider-cost source classes, retrieval gateway, static
   retrieval, isolated browser worker, persistent cache/index, citations,
   source health/circuits, monitoring, concurrency, and cost controls.
4. Prove live-route recall >= 0.35, precision >= 0.60, p95 <= 4 seconds,
   unique contribution on >= 30% of focused misses, <= 70% largest-source
   share, bounded source deadlines, and joined cancellation.
5. Prove 20/20 browser startups and clean teardowns, zero retained state or
   orphans, at least ten lawful JavaScript-required public fixtures, >= 95%
   success, p95 <= 6 seconds, digest attestation, stopped-runtime honesty, and
   the complete containment/SSRF/resource-limit suite.
6. Prove 100% hostile-page boundary behavior; connected cache/freshness,
   locale disclosure/honoring, exact evidence/citations, route and domain
   concurrency, quotas, browser-minute and operation ceilings, circuits,
   half-open restoration, timeout storm, all-routes-down, global traffic stop,
   and no abandoned/unbounded work.
7. Freeze the final implementation and evaluator, execute the final corpus
   exactly once, preserve raw responses/hashes, and perform no tuning afterward.
8. Run focused N4.27S and N4.27R regressions, contract/discovery validation,
   typecheck, lint, secret scan, clean-room verification, artifact/gzip,
   deployment/monitoring, and diff checks. Do not run canonical `npm test`.
9. Bind final evidence and run the Stage 4 verifier exactly once. Payment-bound
   blockers remain blocked. Close only checks proven by real staging evidence.
10. Remove all active compute, prove USD 0/day active exposure, append the
    journal, update handoff/authority, commit atomically, and stop.

## Final quality gates

Combined recall >= 0.90; precision >= 0.86; citation validity >= 0.98;
nDCG@10 >= 0.86; MRR@10 >= 0.83; success@3 >= 0.88; successful extraction
>= 0.95; each family recall and precision >= 0.82; combined retrieval quality
>= simple combination + 0.03 and greater than focused alone; non-browser
balanced p95 <= 2 seconds; honest no-result/degradation and prompt-injection
boundary both 100%. Latency scenarios run at least three times, separately
from the one-run quality corpus, with median and variance.

## Stop condition

Commit N4.27S atomically and stop. Do not start payment work, N4.28, Stage 5,
or any later stage.

## Recorded result

- The 55-task final staging corpus executed exactly once after implementation
  and evaluator freeze. No tuning or rerun followed.
- Repaired combined achieved 0.8000 recall, 0.6803 precision, 1.0000 citation
  validity, 0.8048 nDCG@10, 0.8000 MRR@10, 0.8182 success@3, 0.8727
  extraction and 1465.432 ms network-inclusive p95. Mandatory quality failed.
- Aggregate live routing passed, but developer registry produced zero relevant
  final-corpus contribution, so complete per-source qualification failed.
- Browser qualification failed at 4/20 startups and 0/8 hostile runs; all 20
  teardown checks were clean and final retained state/orphans were zero.
- Cache, locale, SSRF/safety, baseline improvement and operational controls
  produced staging proof. A transient GKE/VM overlap exceeded the configured
  daily-exposure ceiling, so `cost_caps` remains blocked.
- Five Stage 4 blockers closed and five remain. Payment-related blockers remain
  untouched. All ticket resources were deleted and active exposure is USD
  0/day; provider cost and USDC spend are zero.
- Exact proposed next ticket: N4.27T under separate authority. N4.28 and Stage
  5 remain unauthorized.
