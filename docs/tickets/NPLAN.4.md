> **Historical restoration notice (Gate 4.5):** This file preserves earlier
> project history at its original path. Its original status and instructions
> below are historical metadata only. It is not part of the active authority
> chain and cannot authorize work. The Gate 4.5 six-family correction controls
> every conflict.

# NPLAN.4 — Standing autonomous completion dispatcher and owner package

- **Stage:** owner-authorized control-plane amendment while Stage 4 remains active
- **Starting commit:** `bc92aa2a3309815d3008220fe2a284cf22111dc6`
- **One question:** Can Clervo proceed through exact sequential and repair tickets
  without repeated owner approval while keeping external effects, secrets,
  payment, costs, stage gates, sealed evidence, and public claims fail closed?

## Authority and scope

The owner explicitly instructed Codex to create the exhaustive owner-only input
list, safely prepare receiver/payer x402 access, replace per-ticket approval with
a self-managed exact-ticket system, use repair tickets for failures, keep paid
API spend at zero, and work toward truthful completion of the all-six roadmap.

Allowed in NPLAN.4:

- external master plan, `AGENTS.md`, autonomy, canonical product authority, and
  active README/builder/prototype/marketing/use-case transition wording;
- internal dispatcher policy/state, owner/external/payment templates, verifier,
  focused tests, package/acceptance wiring, operations documentation;
- NPLAN.4 decision/ticket/evidence, active state, and append-only journal.

Prohibited in NPLAN.4:

- product/Search runtime changes or N4.27T implementation;
- Stage 4 evidence/binding/verifier or any sealed/final corpus/evaluator change;
- provider/product calls, credentials, cloud/IAM/billing/deployment mutation;
- production, domain, registry, external message/customer, legacy mutation;
- wallet signer access, payment authorization, facilitator call, or USDC spend;
- lifecycle, readiness, reference-pattern, stage-gate, demand, or revenue claims.

Official x402 documentation may be read at zero cost to ensure the template
matches current wallet roles, v2 headers, CAIP-2 network identity, facilitator,
and testnet-first guidance.

## Acceptance

1. Exact-ticket implementation remains mandatory, but per-ticket owner approval
   is false for tickets admitted by the standing dispatcher.
2. Each worker handles one ticket and stops; a fresh cycle may admit the next
   smallest exact ticket after clean closeout.
3. Automatic repair is bounded, preserves failure, protects sealed evidence,
   and escalates repeated same-cause failure to an architecture decision.
4. Paid/eventually-paid/trial-to-bill required API cash spend is exactly USD 0;
   unmanifested external spend is USD 0; infrastructure is separately bounded.
5. External effects remain blocked without exact finite owner-prepared inputs
   and separate explicit owner authority; current workspace manifests reject
   `authorized` until trusted signed enforcement exists.
6. The owner package covers every known owner-only trust-root, identity, legal, account,
   terms, data-rights, cloud, cost, registry, domain, alert, product policy,
   wallet, signer, payment, customer, and interactive-human class.
7. The x402 template requires separate public payer/receiver identities, never a
   receiver private key, at most one real proof, exact network/asset/facilitator/
   amount/evidence/reconciliation, and quarantine on unknown settlement.
8. Current lifecycle remains Search `preview`, five pillars `unavailable`; five
   Stage 4 blockers, Stage 5 false, payment false, and First Revenue Release false.
9. Completion state derives from eight ordered evidence references and hashes;
   lifecycle or stage booleans alone cannot declare the release finished.
10. The master plan, repository authority sources, dispatcher JSON, operations
   docs, and tests agree.
11. Focused verifier/tests, JSON parse, lint, typecheck, secret scan, clean-room
    boundary, and diff integrity pass. Canonical acceptance and the Stage 4
    verifier do not run in this governance ticket.

## Expected transition

After the atomic NPLAN.4 commit and post-commit verification, the worker stops.
A fresh dispatch cycle admits N4.27T's repository-local phase under its already
recorded owner authorization. N4.27S final evidence remains read-only. The
N4.27T cloud phase remains `blocked_owner` until the exact cloud project,
identity, resource allowlist, cost ceilings, no-IAM/billing boundary, cleanup,
and unknown-outcome input is complete and separately explicitly authorized.
Unattended admission additionally requires the owner-controlled signed trust
boundary. Payment is not in N4.27T scope.
