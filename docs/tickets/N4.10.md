# N4.10 — Wire the complete recorded search pipeline through HTTP

## Ticket

- **Stage:** 4 — Search vertical slice.
- **One question:** Can an injected repository-local executor compose N4.1–N4.7 and complete through the N4.9 free and explicit test-only mock-paid loopback HTTP paths without selecting a live provider or enabling real payment?
- **Inputs allowed:** repository-local contracts, recorded adapter metadata and bytes, injected DNS/transport/model responses, worker threads, loopback HTTP, and mock payment evidence. No external provider/model/browser traffic, credentials, cloud/IAM/deployment mutation, facilitator/wallet use, real payment, or USDC spend.
- **Freshness date:** 2026-07-30.
- **Secrets involved:** none.

## Decision

Implement one typed recorded executor under `services/search`, extend the HTTP and mock-commerce seams to await asynchronous execution safely, coalesce concurrent idempotent requests, and prove complete qualification/federation/fetch/extraction/deduplication/ranking/citation/synthesis behavior through loopback HTTP.

## Build output

- Recorded two-path qualification, adapter responses, safe injected transport bytes, deterministic synthesis, and an executor exposing test-only immutable run evidence.
- Actual N4.3 bounded fetch, N4.4 worker extraction/content deduplication, N4.5 federation, N4.6 assembly/ranking/citations, and N4.7 synthesis composition.
- Async free and mock-paid HTTP execution with in-flight idempotency coalescing and unchanged non-payable default behavior.
- Five focused end-to-end tests plus canonical acceptance wiring.

## Validation contract

- **Focused tests:** `npm run test:n2.2`, `npm run test:n4.9`, and `npm run test:n4.10`.
- **Failure test:** one recorded path failing prevents a false complete result, returns a safe 502 code, and exposes no adapter exception detail.
- **Acceptance command:** `npm test`, `npm run staging:smoke`, `npm run verify:boundary`, `npm run scan:secrets`, and `git diff --check`.
- **Cost/network effects:** repository-local execution and loopback HTTP only; no external traffic and 0 USDC spent.
- **Parking lot:** live provider qualification and provider-to-fetch adapters; durable distributed idempotency/quota/commerce state; HTTP disconnect cancellation; calibrated/live quality; public staging listener and Stage 4 exit proof; real x402.

## Completion

- **Result:** verified for the repository-local recorded boundary. Focused N2.2 passed 8/8, N4.9 passed 5/5, N4.10 passed 5/5, and canonical acceptance passed 136/136 with 29 schemas and 54 fixtures under Node.js 24.18.1.
- **Exact next ticket:** N4.11 — perform a bounded Stage 4 staging/exit verification and decide whether search may become the reference pattern before Stage 5 begins. This remains paused until explicitly authorized.