# N4.27R — Search generalization, live-route and browser reliability repair

Status: completed at controlled repair-entry level on 2026-07-31. This file records the owner-authorized scope supplied after the blocked N4.27 closure at commit `4ee469c2041af455281c131c0a090bcf31f1095a`.

## Objective

Repair the causes of N4.27's failed generalization and prove, on a new development corpus and a once-only sealed validation set, that the combined search pipeline adds measurable retrieval value over the same raw components. Preserve the original N4.27 holdout and every recorded hash unchanged.

## Required sequence

1. Produce a per-task machine-readable root-cause ledger from the existing N4.27 raw artifact without rerunning it.
2. Before implementation changes, create and hash a separate 75-task N4.27R corpus with 15 tasks in each product family, split into a repeatable 50-task development set and a once-only 25-task sealed validation set.
3. Repair candidate retention, deterministic rank fusion, score calibration, duplicate handling, live-source deadlines/quotas/circuits/contribution accounting, locale behavior and honest no-result lifecycle without query-specific rules.
4. Repair browser startup and teardown reliability while preserving gateway-only default-deny networking, non-root execution, read-only-root design, ephemeral state, bounded resources, no stealth, no proxy rotation, no login/cookies/downloads and no caller scripts.
5. Complete controlled hostile-page and network-denial proof.
6. Tune only on the development split. Freeze and hash implementation files. Execute the sealed validation exactly once. Do not tune after that execution.
7. Run focused validation, typecheck, lint, secret scan, clean-room verification, artifact integrity and git-diff checks. Do not run canonical `npm test`.
8. Bind evidence, update the master plan, ticket, journal and handoff, commit N4.27R atomically, then stop.

## Repair-entry gates

Development: recall >= 0.92; precision >= 0.88; citation validity >= 0.98; nDCG@10 >= 0.88; MRR@10 >= 0.85; success@3 >= 0.90; every family recall and precision >= 0.85; combined retrieval quality >= simple combination + 0.03; no unexplained relevant-result loss.

Live: recall >= 0.35; precision >= 0.60; p95 <= 4 seconds; unique relevant contribution in at least 30% of focused misses; the combined route remains within its merge deadline; suspended sources fail quickly and independently; no source dominates successful contributions.

Browser: 20 consecutive successful startups and clean teardowns; zero orphan processes or retained state; at least 10 JavaScript-required fixtures; success >= 95%; p95 <= 6 seconds; complete private/loopback/link-local/metadata, redirect, DNS-rebinding, robots, MIME, decompression, output-limit and stopped-runtime proof.

Sealed validation: recall >= 0.90; precision >= 0.86; citation validity >= 0.98; nDCG@10 >= 0.86; MRR@10 >= 0.83; success@3 >= 0.88; every family recall and precision >= 0.82; combined retrieval quality >= simple combination + 0.03; non-browser balanced p95 <= 2 seconds; browser and security gates pass.

These are repair-entry gates only. They do not authorize commercial-superiority claims.

## Hard prohibitions

- Never execute, alter, relabel or tune to the frozen N4.27 holdout.
- Never run mock x402, spend USDC, use a paid or eventually-paid general-Web search provider, or touch the reserved 0.03 USDC.
- Never begin N4.28, Stage 5, website work, public DNS work or real payment.
- Never claim Exa parity, best Web search, advanced live intelligence, reference-pattern authorization or Stage 5 authorization.
- Do not create cloud infrastructure or retain active compute. General-Web provider production cost and active incremental daily exposure remain USD 0.000000.
- Do not run the Stage 4 verifier; this authoritative ticket does not require it.
- Preserve the clean-room repository boundary and introduce no external runtime dependency.

## Allowed implementation boundary

`packages/contracts`, `services/search`, `adapters/search`, `infra/n4.27r`, `scripts/benchmarks/n4.27r`, focused N4.27R contract tests, `benchmarks/n4.27r`, `docs/evidence/n4.27r`, this ticket, the launch/master-plan state, the append-only journal and active handoff. Existing N4.26/N4.27 artifacts are read-only evidence.

## Closure

Completed without rerunning or modifying the original N4.27 holdout. The new 50-task development split and once-only 25-task sealed validation passed every N4.27R repair-entry quality gate. Controlled browser/security qualification passed 20/20 startups and clean teardowns, complete destination/redirect/rebinding/robots/MIME/decompression/output denials, and eight hostile-page runs.

This result is intentionally bounded. No N4.27R cloud resource or production source connection was created; no Stage 4 evidence was promoted; no Stage 4 verifier or canonical `npm test` ran; mock x402 and real payment remained unexecuted; provider production cost and active incremental exposure remained USD 0.000000; USDC spend remained zero. All 10 source-bound Stage 4 blockers remain, so commercial competitiveness, Exa parity, advanced live intelligence, reference-pattern and Stage 5 authorization remain false.

Exact proposed next ticket: N4.27S — Frozen-repair staging qualification and remaining non-payment Stage 4 proof. It remains unauthorized.
