# N4.11 — Perform bounded Stage 4 staging/exit verification

## Ticket

- **Stage:** 4 — Search vertical slice.
- **One question:** Does current evidence prove every §7.1 search requirement in staging strongly enough to authorize search as the reference pattern and begin Stage 5?
- **Inputs allowed:** checked-in repository evidence, generated discovery/OpenAPI, staging environment/release manifests, local validation, and existing N4.1–N4.10 results. No invented live evidence, credential use, provider/model/browser traffic, cloud/IAM/deployment mutation, wallet/facilitator use, real payment, or USDC spend.
- **Freshness date:** 2026-07-30.
- **Secrets involved:** none.

## Decision

Build a deterministic Stage 4 exit manifest and verifier covering every §7.1 requirement and gate. Keep repository-local evidence separate from staging evidence, bind source-state assertions to checked-in artifacts, and authorize the reference pattern and Stage 5 only if every named check is staging-verified.

## Build output

- A 22-check Stage 4 exit evidence manifest under `infra/staging`.
- A source-bound verifier that recomputes the exit decision and refuses unsupported promotion.
- Four adversarial tests covering the truthful blocked result, forged pass claims, incomplete coverage, and invented deployment state.
- Canonical acceptance wiring and completion records without changing discovery, payment, deployment, or product behavior.

## Validation contract

- **Focused test:** `npm run test:n4.11`.
- **Failure test:** a forged passed decision, missing §7.1 check, or source-state mismatch fails closed.
- **Acceptance command:** `npm test`, `npm run staging:smoke`, `npm run verify:boundary`, `npm run scan:secrets`, and `git diff --check`.
- **Cost/network effects:** repository-local evaluation and loopback staging hello smoke only; no external traffic and 0 USDC spent.

## Completion

- **Result:** N4.11 verification passed and the Stage 4 exit decision is **blocked**. All 22 named checks lack staging verification; search is not authorized as the reference pattern and Stage 5 is not authorized.
- **Focused validation:** `npm run test:n4.11` passed 4/4.
- **Canonical validation:** acceptance passed 140/140 with 29 schemas and 54 fixtures under Node.js 24.18.1/npm 10.9.8. Loopback staging smoke, clean-room boundary, working-tree/full-history secret scan, and diff checks passed.
- **Deployment checked:** checked-in staging/discovery state plus the existing local hello smoke. Staging remains `not-provisioned`; no public search listener, live provider, browser, telemetry delivery, real payment, or remote deployment was exercised.
- **Exact next ticket:** N4.12 — implement and test bounded query rewriting as the first named Stage 4 remediation. It remains paused until explicitly authorized.