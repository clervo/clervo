# N3.2 — Build health, routing, budgets, and circuits

## Ticket

- **Stage:** 3 — Reliability and observability
- **One question:** Do provider, database, facilitator, and network failures degrade correctly, and do hard quota/cost switches stop traffic?
- **Inputs allowed:** controlling plan; repository-local catalog, adapter, qualification, commerce, mock-kernel, and observability contracts; bounded circuit-breaker guidance. No legacy runtime or live dependency.
- **Freshness date:** 2026-07-30.
- **Secrets involved:** none.

## Decision

Add deterministic fail-closed routing with freshness-bound health evidence, rolling-window circuits, exact qualification/product/deadline gates, atomic request-and-cost budget reservations, and pre-consumption-only failover.

## Build output

- Health/dependency gates, circuit transitions, budget reservations, deterministic route decisions, and failover disposition contracts.
- One strict Draft 2020-12 route-decision schema with positive and adversarial fixtures.
- Eight focused tests covering selection, provider failover, dependency failure, hard switches, reservation finalization, circuit recovery, failed probes, and unknown-outcome quarantine.
- Decision record, ticket evidence, journal/README updates, generated public schema refresh, and next-ticket handoff.

## Validation contract

- **Focused tests:** `npm run contracts` and `npm run test:n3.2`.
- **Failure test:** stale/unknown provider evidence, unavailable required dependencies, open circuits, exceeded cost/quota, insufficient deadlines, and unknown/possibly-consumed outcomes must not authorize unsafe provider traffic.
- **Acceptance command:** `npm test` plus `npm run staging:smoke`.
- **Cost/network effects:** bounded read-only standards request and local tests only; no provider, facilitator, wallet, cloud, IAM, deployment, payment, or traffic mutation; 0 USDC.
- **Parking lot:** durable PostgreSQL reservations/circuits, multi-worker locking, crash recovery, live health probes, real provider traffic, alert delivery, and remote staging.
- **Stop condition:** focused and repository-wide acceptance pass, evidence is recorded, and the controlling-plan handoff names N4.1.

## Completion

- **Result:** verified for deterministic repository-local contracts.
- **Files changed:** routing/budget/circuit TypeScript and exports; one strict schema; two fixtures; eight tests; package/test wiring; generated public schema/OpenAPI; decision, README, journal, and controlling-plan evidence.
- **Focused tests:** `npm run contracts` passed with 19 schemas and 35 fixtures; `npm run test:n3.2` passed 8/8.
- **Repository-wide tests:** lint/typecheck, clean-room/stack/environment gates, N0.3 negative control, N1.1 through N3.2 (52/52 tests), deterministic discovery, loopback staging smoke, current-delta secret scan, schema semantic parity, and diff checks passed.
- **Deployment checked:** existing local loopback staging hello smoke only; no live provider, facilitator, PostgreSQL, pg-boss, payment, cloud, IAM, or remote deployment was exercised.
- **Claims still unknown:** durable and concurrent state, crash recovery, live failure behavior, routing HTTP integration, and production availability.
- **Exact next ticket:** N4.1 — define and implement the first bounded advanced-search slice from Stage 4.