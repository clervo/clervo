# N4.24 independent live-federation and connected-pipeline evidence

- Evaluated: 2026-07-31
- Starting commit: `d3c2d3913d64642161b76a510da4faed310907b`
- Route identity: `clervo.live-federation.v1`
- Third-party search-provider API cost: USD 0.000000
- External source-review calls: bounded official documentation only; no search/API/content probe
- Credentials/secrets: none inspected, used, or printed; USDC spent: 0
- Stage effect: 21 blockers before and after; no `stagingVerified` value changed

## Selected open-data adapters and terms conclusions

| Source | Exact identity | Terms/use conclusion |
| --- | --- | --- |
| Wikimedia Action API | `provider_wikimedia_action_api_v1` / `adapter_wikimedia_action_api_v1` | Qualified metadata/current-page discovery only with meaningful contact User-Agent, API limits/maxlag, page URL attribution, and page-level license/ShareAlike compliance. Media without item-license review and unattributed content are rejected. |
| Crossref REST API | `provider_crossref_rest_api_v1` / `adapter_crossref_rest_api_v1` | Qualified bibliographic metadata. Polite-pool `mailto` and client identity are required; abstracts and publisher full text are excluded because rights can belong to publishers/authors. DOI/publisher URL remains attribution. |
| Common Crawl CDXJ/index | `provider_common_crawl_index_v1` / `adapter_common_crawl_cdxj_v1` | Metadata-only provisional discovery. Official index/terms pages document bounded index access and overload limits; archived WARC/WAT/WET bodies are not qualified and are rejected from output. Current URL must be refetched through Clervo safety controls. |

Official pages reviewed (2026-07-31): Wikimedia Terms of Use (`https://foundation.wikimedia.org/wiki/Policy:Terms_of_Use`, SHA-256 `7c6af9f1655422d1e58b2d4d97890a88065ff376c75e750f716f8807d4e88954`), MediaWiki API etiquette (`https://www.mediawiki.org/wiki/API:Etiquette`, SHA-256 `9dd66b6eae2f4e707d40123160f173cc548cd92b8891fa61494dcfacead2e20f`), Crossref REST API (`https://www.crossref.org/documentation/retrieve-metadata/rest-api/`, SHA-256 `32ceaf90269046a7bf55fdedb725d304909afac6e76dda8e1016a6d83f3898ba`), Common Crawl Terms (`https://commoncrawl.org/terms-of-use`, SHA-256 `4929d4e006539854a0f66eec64704e5a98d7382a6f3ce0a82c1ad5f1e72c9ab3`), and Common Crawl index (`https://index.commoncrawl.org/`, SHA-256 `7beb65c8ff3f5ed6b0744426593bfae7f2d6640bb8927ef79ac0a07839fe4c8c`). Two initially guessed Crossref deep-link URLs returned 404 and were not used as evidence; the current official REST API page was used instead.

## Independence proof

- Focused route remains `clervo.focused-index.v1` with Meilisearch provider `provider_meilisearch_1_51_0`, adapter `adapter_meilisearch_focused_1_51_0`, health `clervo.health.focused_index`, circuit `clervo.circuit.focused_index`, and failure domain `clervo.focused_index`.
- Live route is `clervo.live-federation.v1` with provider `provider_clervo_live_federation_v1`, adapter `adapter_clervo_live_federation_v1`, health `clervo.health.live_federation`, circuit `clervo.circuit.live_federation`, and failure domain `clervo.live_federation`.
- Live route has no focused-index frontier, Meilisearch index, focused health state, or focused circuit state. Constructor identity checks reject substitution; no silent fallback crosses route identities.

## Proved local behavior

- Deterministic identity/exact-phrase query rewrite; both routes start concurrently under one absolute deadline, linked cancellation, and bounded route attempts.
- Independent route failure, timeout, cancellation, both-failed fail-closed behavior, exact identity accounting, locale propagation, and honest ready/degraded status.
- URL normalization, exact and five-word-shingle near-duplicate suppression, deterministic freshness/authority/relevance/diversity ranking, replay stability, and evidence-offset citation verification/mismatch rejection.
- Direct current-page fetch reuses URL/DNS/robots/redirect/MIME/byte/deadline controls and rejects archived WARC/WAT/WET targets.
- Common Crawl parser accepts CDXJ metadata only and rejects body/payload keys. Prompt-like source text remains `untrusted_data_only` evidence.
- Crawl4AI fallback selects only deterministic JavaScript-required pages and enforces pinned `0.9.2`/`1.61.0`, disposable internal provisional state; N4.25 production isolation remains unproven.
- Local `search.web`, `web.fetch`, and `web.extract` operate through injected loopback fixtures. `search.answer` stays preview-only; `research.report` stays unavailable.

## Validation

- `npm run test:n4.24`: 12/12 passed.
- `npm run test:n4.23b`: 8/8 passed (regression check).
- `npm run contracts`: 40 schemas / 75 fixtures passed.
- `npm run generate:discovery`: 40 schemas generated.
- Typecheck/build passed; no canonical `npm test` was run (reserved for N4.28).
- No staging deployment or source-bound evidence promotion occurred.

## Cost, network, and credentials

- Official documentation hosts contacted only: `foundation.wikimedia.org`, `www.mediawiki.org`, `www.crossref.org`, `commoncrawl.org`, and `index.commoncrawl.org`; no provider search/API endpoint, current publisher page, cloud service, or paid route was probed.
- Provider API cost: USD 0.000000. Infrastructure cost: USD 0.000000 (loopback/injected local fixtures only).
- Credentials: none. Wallet/facilitator/payment/deployment/IAM/billing actions: none. USDC spent: 0.

## Claims still unknown

Production supplier qualification/resale, live quality/latency, staging traces, durable cache, Crawl4AI process/network isolation, monitoring delivery, hard live-provider cost caps, payable routing, real payment, and Stage 4 exit remain unproven. Stage 4 remains blocked on 21 checks; reference-pattern and Stage 5 authorization remain false.
