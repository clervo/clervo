# N4.27 quality, browser, security, cost, and blocked-decision evidence

- Evaluated: 2026-07-31
- Starting commit: `4712a7f2ff69eea03ecf584f9902a200cc8b24ff`
- Result: `blocked`
- Frozen holdout executions: exactly one final run, 900 scenario/task/repetition rows
- Search/reference-pattern authorization: false
- Stage 5 authorization: false
- Mock x402: prerequisite failed; never started
- Real payment: prohibited and unexecuted
- USDC spent: 0
- Third-party general-Web search-provider production cost: USD `0.000000`

## Decision

N4.27 does not qualify Search as commercially competitive and does not close
Stage 4. The repaired implementation improved the N4.26 regression, but it
failed the independent frozen holdout, browser reliability, complete security,
cache, locale, duplicate-classification, token-retention, source-quality and
complete cost-control gates. All ten inherited Stage 4 blockers remain open.

The strongest authorized positioning remains **“Clervo-owned live Web
retrieval.”** N4.27 does not authorize “Advanced live intelligence for
agents,” Exa parity or superiority, highest-performing-system language,
reference-pattern status, payable readiness, N4.28, or Stage 5.

## Frozen benchmark integrity and regression ledger

The corpus, labels, evaluation rules, source observations and implementation
were frozen before the final holdout. Their SHA-256 values match both freeze
manifests. `benchmarks/n4.27/holdout-final-run.v1.json` records `runCount: 1`
at `2026-07-31T19:34:33.784Z`; the final evaluator refuses any second run.
This holdout must never be rerun or relabelled.

The N4.26 loss ledger accounts for every one of 28 simple-to-combined losses:
25 were classified as non-query-discriminating relevance/ranking losses and
three as live extraction failures. There are zero unexplained losses and zero
losses attributed to deduplication.

The final N4.27 regression improved combined recall from 0.8333 to 0.8864,
precision from 0.8144 to 0.8826, citation validity from 0.9091 to 0.9545 and
successful extraction to 0.9545. Combined p95 increased from 1630.51 ms to
2404.62 ms. The regression gate still failed because mandatory family and
other quality requirements were not all satisfied.

## Frozen holdout result

| Metric | Repaired balanced | Required | Result |
| --- | ---: | ---: | --- |
| Recall | 0.04 | 0.92 | fail |
| Precision | 0.04 | 0.88 | fail |
| Exact citation validity | 1.00 | 0.98 | pass |
| Structured-field accuracy | 0.92 | 0.90 | pass |
| Successful extraction | 0.92 | 0.95 | fail |
| nDCG@10 | 0 | 0.88 | fail |
| MRR@10 | 0 | 0.85 | fail |
| Success@3 | 0 | 0.90 | fail |
| p95 latency | 4688.24 ms | 2000 ms maximum | fail |

All five family holdout floors failed. Thorough mode did not improve recall
over balanced mode. The repaired system did not establish a commercially
useful ranking result on the frozen holdout.

## Browser and security qualification

Crawl4AI `0.9.2` and Playwright `1.61.0` ran in the pinned non-root browser
image with gateway-only egress, a read-only root, ephemeral writable paths,
one-page execution, hard process/memory/CPU/output/source/deadline limits,
disabled downloads, no persistent session, no caller JavaScript or hooks, and
no stealth or proxy rotation.

Exactly 20 consecutive runs were attempted. Eighteen returned the expected
JavaScript markers and successful teardown receipts with zero surviving
orphans; runs 12 and 18 failed with `crawl4ai_render_failed`. The observed
success rate was 0.90 and successful-run p95 was 2580.911 ms, but the mandatory
20/20 startup and teardown gate failed.

All tested loopback, RFC1918, link-local, metadata, private-resolution and
redirect-to-private gateway denials returned 403. Kill-switch and stopped-
worker behavior passed. The mandatory security suite nevertheless remains
incomplete because controlled DNS rebinding, complete robots denial,
MIME/decompression adversarial retrieval, and hostile-page prompt-injection
staging execution were not completed.

## Source, cache, locale, deduplication, and token evidence

The live path retained distinct Wikimedia and Crossref failure identities and
zero third-party general-Web provider production cost. Regression live recall
was 0.4318, but precision was 0.428 and Crossref represented 0.72 of relevant
contributions. Frozen-holdout live recall and precision were 0.08. Source
quality and concentration gates failed.

Connected-cache repository tests cover miss, fresh hit, stale revalidation,
disclosed stale degradation, eviction, denylist invalidation, route/URL/policy
keys, cross-route rejection and integrity failure, but the full staging
transcript is absent. Locale propagation scored 1.0 in the evaluators, while
upstream locale honoring and silent-substitution exclusion remain unproven.
Hostile-page staging execution did not occur.

The deduplication implementation retained the stronger canonical
representative and distinct sellers in focused tests, but the frozen corpus
did not yield a numeric false-positive/false-negative confusion matrix. The
bounded evidence form reduced characters by 98.984%, but relevant-evidence
retention is null because the repaired balanced path found no labelled relevant
evidence set. Neither mandatory gate passes.

## Cost, cleanup, and commerce stop

The conservative N4.27 gross economic upper bound is USD 0.25. Credits are not
assumed to reduce economic cost. The isolated VM and auto-delete disk, NAT and
router, firewall, subnet/network, service-account IAM binding, fixture service,
N4.27 artifact images and ticket budget were deleted. The evidence records no
retained N4.27 resources, active compute exposure USD 0/day, and retained idle
exposure USD 0/day.

Because mandatory prerequisites failed, mock x402 did not begin: no challenge,
authorization, verification, mock settlement, receipt or replay occurred. The
reserved wallet was untouched, no route became payable, and no USDC was spent.

## Smallest exact repair ticket

**N4.27R — Repair representative lawful retrieval and deterministic browser
qualification.** Replace the narrow live supply/index with lawful vertical
source adapters and a representative focused index; add labelled duplicate and
hostile-page fixtures; make browser fixtures local and deterministic; then
freeze a new independent holdout and run the complete N4.27 gates. N4.27R must
not reuse or rerun the N4.27 holdout, begin N4.28, begin mock x402 before every
prerequisite passes, authorize a reference pattern, or begin Stage 5.

## Bound evidence

`docs/evidence/n4.27/stage4-binding.v1.json` binds the freeze records, the
one-shot marker, all three regression attempts, loss ledger, final holdout raw
output and scorecard, browser/security/source/cache/locale/ranking/token/cost
evidence, mock-x402 stop, mandatory-gate summary and blocked claim decision.
The Stage 4 manifest preserves the same ten blockers and false reference-
pattern and Stage 5 authorizations.

Focused N4.27 tests passed 10/10; N4.24/N4.25/N4.26 regressions passed 33/33;
the N4.22 campaign consistency regression passed 5/5; typecheck and lint across
236 source/contract files passed; the full secret scan,
clean-room boundary, JSON/gzip/hash integrity and diff checks passed. The Stage
4 verifier ran exactly once after final evidence binding and passed verifier
integrity with decision `blocked`, 10 blockers, 23 N4.27 bound artifacts,
reference-pattern authorization false, Stage 5 authorization false, zero
external calls and 0 USDC. Canonical `npm test` was not run.
