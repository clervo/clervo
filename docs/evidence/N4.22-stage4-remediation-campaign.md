# N4.22 Stage 4 remediation campaign matrix and external gate

- Evaluated: 2026-07-31
- Starting commit: `0060277`
- Machine-readable matrix: `infra/staging/stage4-remediation-campaign.json`
- Deterministic verifier: `scripts/verify-stage4-campaign.mjs`
- Result: 21 exact source-bound blockers remain; N4.23 is blocked by owner/external prerequisites
- Stage 5: not started
- Payment/USDC: none / 0 USDC

## TypeScript editor warning

The repository has workspace TypeScript `7.0.2` and `tsconfig.json` targets `ES2023`. `npm run typecheck` passed with the pinned Node.js 24.18.1 runtime. The workspace compiler therefore supports `ES2023`; the Mac VS Code warning comes from an older bundled/selected editor TypeScript language service. No repository dependency or `tsconfig.json` change was needed. VS Code should use “TypeScript: Select TypeScript Version → Use Workspace Version.”

## Verifier semantics

`npm run verify:stage4-exit` loads the checked-in Stage 4 manifest, binds six deployment/discovery source-state fields to actual files, requires all 22 §7.1 check identifiers, and counts every check with `stagingVerified=false` as blocking. It does not allow repository-local proof to substitute for staging evidence. `deployed_free_sample` is the sole staging-verified check; the following 21 remain blocking.

## Compact blocker matrix and dependency queue

| Queue group | Exact blocker identifiers | Required source-bound evidence | Current missing evidence | Boundary |
| --- | --- | --- | --- | --- |
| N4.23 — lawful supply and staging access | `federated_lawful_retrieval`, `two_independent_retrieval_paths` | Two exact independent production/resale-eligible retrieval suppliers executing through the product in authenticated staging with isolated failure and provenance | Public Nominatim is non-resale; Common Crawl is legal-review gated; neither development path is product-connected; no staging identity is available | Owner decision + local integration + staging |
| N4.24 — live product pipeline | `query_rewriting`, `parallel_retrieval`, `url_normalization_and_near_duplicate_removal`, `freshness_authority_relevance_diversity_ranking`, `prompt_injection_boundaries`, `evidence_tied_citations`, `language_and_region_options`, `deterministic_schema`, `citation_verifier` | Release-bound staging traces for qualified providers through rewrite, parallel fetch, normalization/deduplication, ranking, locale, injection isolation, exact citations, schemas, and tamper rejection | All mechanisms are repository-verified, but no qualified live provider-to-product path or deployed trace exists | N4.23 + local + staging |
| N4.25 — browser, cache, and security | `isolated_javascript_retrieval`, `retrieval_safety_controls`, `disclosed_cache_freshness`, `ssrf_and_security_suite` | Concrete disposable browser execution, durable environment-separated cache miss/hit/revalidation, and authenticated staging safety/SSRF drills | Browser remains injected-only; cache is process-local; security suite has no deployed live transcript | N4.24 + local + staging |
| N4.26 — live quality and operations | `quality_latency_and_cost_benchmarks`, `blockrun_compatible_baseline_improvement`, `monitoring`, `cost_caps` | Calibrated live benchmark, baseline improvement, queryable metrics/alert delivery, and provider traffic-stop drills for budget/circuit/kill-switch bounds | Benchmark is synthetic; monitoring is process-local plus request logs; no live provider cost/cap drill or alert channel exists | N4.25 + owner channel/provider authority + local + staging |
| N4.27 — payable route | `separate_raw_and_synthesis_prices`, `deployed_paid_route` | Deployed payable x402 challenge, verification, execution, safe settlement/reconciliation, exact price-bound receipts, and replay without second charge | Discovery says payment is unimplemented; deployed route is deliberately non-payable; facilitator/payee authorization is absent and this campaign prohibits wallet/facilitator transactions and USDC spend | Owner payment/facilitator authorization + local + staging |

The machine-readable matrix contains one individual record per identifier with its exact required evidence, current gap, dependency group, and local/staging/owner boundary. N4.28 is the final zero-blocker verification group and depends on N4.25–N4.27.

## Staging access evidence

- Target: `bloxsniper-prod`, `us-central1`, `clervo-stage4-slice-staging`.
- Existing release: private/authenticated recorded-only N4.18 release `2f6fd6c`.
- `gcloud auth print-access-token`: unavailable because no active account is selected.
- Application-default credentials: absent.
- Relevant credential environment-variable names present: none.
- Repository-supported credential names: `GOOGLE_APPLICATION_CREDENTIALS`, `CLERVO_STAGING_IDENTITY_TOKEN`.
- One bounded unauthenticated `GET /v1/health` returned HTTP 403, 304 bytes, SHA-256 `7e003d2f633eeeb1b8536c627a1adeb02fe4eaa9487e631f91d8e9515043c5fe`.
- Safe failure: no deploy, traffic change, log read, authenticated smoke, discovery promotion, provider call, or staging-evidence promotion was attempted.

## Genuine external blockers and exact owner intervention

1. Activate an authorized gcloud identity or mount approved application-default credentials with Cloud Run, Cloud Build, Artifact Registry, and log read/deploy access for `bloxsniper-prod/us-central1`. Codex must not change IAM.
2. Approve two independent production/resale-eligible general-Web retrieval suppliers and their terms, replacing public Nominatim. Explicitly approve Common Crawl content use or select a legally cleared archive alternative.
3. After supplier selection, provide the approved provider credential environment-variable names and mounted values. No provider credential is currently present.
4. Authorize staging facilitator/payee configuration and any separately bounded real-settlement proof required for `deployed_paid_route`. Current authority prohibits wallet/facilitator transactions and USDC spending.
5. Provide an approved staging alert-delivery channel and its credential environment-variable name if Cloud Logging cannot satisfy the monitoring-delivery gate.

Until these prerequisites exist, local work cannot truthfully reduce the verifier count because every remaining check specifically requires staging verification. Creating more local proof-only tickets would violate the campaign instruction and the N4.11 boundary.

## Network, credentials, deployment, and cost

External host contacted: `clervo-stage4-slice-staging-jbtbib4yqa-uc.a.run.app` once, returning HTTP 403. Credential values read or printed: none. Credential environment-variable names involved: none present; `GOOGLE_APPLICATION_CREDENTIALS` and `CLERVO_STAGING_IDENTITY_TOKEN` are the supported future names. No deployment, staging mutation, provider call, crawling, cloud/IAM/billing change, wallet/facilitator action, payment, or USDC spend occurred.
