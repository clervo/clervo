# N4.11 Stage 4 exit verification decision

Date: 2026-07-30

## Decision

Search does not pass the Stage 4 exit gate and must not become the reference pattern yet. Stage 5 is not authorized.

Encode every §7.1 requirement and gate as a named evidence check, bind the decision to checked-in staging and discovery source state, and fail closed unless every check has fresh staging evidence. Repository-local proof remains useful but cannot be promoted to staging proof.

## Observed boundary

- All 22 named checks lack staging verification.
- Existing work provides repository-local evidence for deterministic schemas, two recorded paths, federation, fetch safety, extraction, deduplication, ranking, citations, synthesis boundaries, synthetic benchmarking, and process-local cost reservation.
- Query rewriting, isolated JavaScript retrieval, robots enforcement, cache-freshness disclosure, language/region controls, separate raw/synthesis pricing, and delivered monitoring remain missing or incomplete.
- The deployed-free-sample gate is contradicted by `liveDeploymentStatus: not-provisioned` and unverified deployment discovery.
- The deployed-paid-route gate is contradicted by discovery stating payment is not implemented and the route is non-payable.
- There is no Git remote or available GitHub/cloud/container deployment control plane in this checkout, so no truthful remote staging verification could be performed.

## Consequence

N4.11 is complete as an exit decision, but Stage 4 remains open. Generated discovery remains `implemented_unverified`; payment and deployment claims remain false. The next bounded ticket is N4.12, beginning with query rewriting rather than Stage 5 RPC work.

## Rejected alternatives

- Treat recorded loopback evidence as staging evidence: rejected because Stage 4 explicitly requires all §7.1 requirements to pass in staging.
- Mark the local free route as deployed: rejected because the release manifest says staging is not provisioned.
- Treat the test-only mock-paid seam as a paid route: rejected because the public route is explicitly non-payable and real payment is not implemented.
- Begin Stage 5 while Stage 4 is blocked: rejected because search has not earned reference-pattern status.

## 2026-07-31 evidence update

N4.18 subsequently verified a private authenticated recorded-only Cloud Run free sample, so `deployed_free_sample` is no longer a blocker. This does not reverse the decision: 21 checks remain without staging verification, including the payable-route, live-retrieval, security, benchmark, monitoring-delivery, and cost-cap gates. Search is still not the reference pattern and Stage 5 remains unauthorized.