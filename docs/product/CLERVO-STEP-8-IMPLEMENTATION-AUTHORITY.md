# Clervo Step 8 implementation authority

- **State:** implementation in progress
- **Issue:** #9
- **Design authority:** `CLERVO-BRAND-WEBSITE-MASTER-VAULT-v4.0-DESIGN-AUTHORITY-LOCKED.zip`
- **Repository truth:** current generated contracts, tests, lifecycle, availability, and engineering state

## Authority resolution

The locked v4.0 vault controls brand position, Hollow Apex identity, semantic red → cyan → gold behavior, typography roles, composition, routes, navigation labels, mobile parity, liquid controls, and approved page hierarchy.

The repository controls factual engineering truth. A prototype fixture never becomes a live product, payable price, production receipt, deployment claim, incident, benchmark, or public availability statement merely because it appears in the design authority.

## Step 8A foundation

This implementation slice:

- replaces the temporary rotated `C` mark with the Step-2-faithful Hollow Apex component;
- establishes the locked semantic tokens and typography roles;
- aligns the global shell to Product · Catalog · Pricing · Docs · Status · Set up Clervo;
- adds the accessible mobile navigation authority;
- canonicalizes `/build` to `/start` and `/proof-lab` to `/proof` without breaking existing internal flows;
- adds Catalog, capability-family, operation-contract, and Changelog routes;
- preserves fixture and non-public lifecycle disclosures;
- adds the Clervo Liquid Capsule control primitive and reduced-motion behavior;
- adds a deterministic authority test.

## Deliberately not changed

- backend operations and APIs;
- wallet, quote, payment, settlement, or receipt implementations;
- generated discovery truth;
- production deployment or public release;
- provider configuration or external control planes.

## Remaining Step 8 work

1. Align every existing page composition to its locked v1.3 reference.
2. Load and validate production Space Grotesk, Inter, and JetBrains Mono assets.
3. Export the final production identity pack: SVG variants, wordmark, favicon, app icon, clear space, and minimum sizes.
4. Expand catalog and operation pages through typed data-source adapters.
5. Add visual-regression baselines at desktop, 390px, and 320px.
6. Run the repository build, prerender, browser, interaction, accessibility, and mobile gates.
7. Open focused follow-up pull requests rather than replacing the complete site in one unreviewable change.
